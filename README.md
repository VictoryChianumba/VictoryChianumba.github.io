# VictoryChianumba.github.io

Minimal Jekyll personal site for portfolio pages and writing, designed for GitHub Pages.

Run locally:

```sh
bundle install
bundle exec jekyll serve
```

Then visit `http://localhost:4000`.
